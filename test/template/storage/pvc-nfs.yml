apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-nfs   # tu peux garder le nom pour éviter de modifier le Deployment
  namespace: dev
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
