apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: nfs-csi
provisioner: nfs.csi.k8s.io
parameters:
  server: 192.168.49.2  # Remplacez par l'adresse IP de votre serveur NFS
  share: /data
reclaimPolicy: Retain
volumeBindingMode: Immediate
allowVolumeExpansion: true