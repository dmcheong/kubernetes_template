apiVersion: v1
kind: Pod
metadata:
  name: nginx-dev
  namespace: developpement
spec:
  containers:
    - name: nginx
      image: nginx:stable
      ports:
        - containerPort: 80
      securityContext:
        privileged: false
        allowPrivilegeEscalation: false
        capabilities:
          drop:
            - ALL